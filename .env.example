DATABASE_URL="postgresql://user:password@localhost:5432/golfpooltracker?schema=public"
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/golfpooltracker_test?schema=public"

# RapidAPI key for the "Live Golf Data" API (slashgolf). When set, pool
# sync actions and the polling script use RapidApiGolfProvider instead of
# the mock provider. Leave unset for local dev without a live feed.
RAPIDAPI_KEY=""
